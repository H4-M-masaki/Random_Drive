
<div class="container">
  <div class="row justify-content-md-center">
   <div class="col-md-auto">
    <h1>ユーザー投稿詳細</h1>
    <div class="p-1 border bg-light">
     <div class="card h-100">
      <div class="card-img-top" alt="card-img-top"><%= attachment_image_tag @post, :image, format: 'jpeg', size: "auto" %></div>
       <div class="card-body">
        <h4 class="card-title"><%= @post.title %></h4>
        <p class="card-text"><%=safe_join(@post.body.split("\n"),tag(:br)) %></p>
        <p class="card-text">ユーザーネーム:<%= link_to @post.user.name, admin_user_path(@post.user_id) %></p>
        <p class="card-text"><small class="text-muted"><%= @post.created_at.strftime("%Y-%m-%d %H:%M") %></small></p>
       </div>
      </div>
     </div>
    </div>
  </div>
</div>